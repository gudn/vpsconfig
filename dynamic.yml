http:
  middlewares:
    admin-auth:
      basicAuth:
        usersFile: /users/admin
    proxy-auth:
      digestAuth:
        realm: proxy
        usersFile: /users/proxy
    compress:
      compress: {}
